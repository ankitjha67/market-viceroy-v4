# ichimoku_cloud — Ichimoku Kinko Hyo (Patel 2010)

> Long when close > cloud top (max of Senkou A and B); short when
> close < cloud bottom; flat inside the cloud.

```python
from alphakit.strategies.trend import IchimokuCloud
strategy = IchimokuCloud()
```

See [`paper.md`](paper.md), [`known_failures.md`](known_failures.md).
