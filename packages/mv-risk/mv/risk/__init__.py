"""mv-risk — Risk engine with inviolable kill-switch, daily-loss/drawdown breakers, and position/exposure caps. INVIOLABLE: no agent or autonomy setting may override these — only the Operator.

Phase: Phase 1. This is a Phase-0 typed skeleton: package boundary, version,
and `py.typed` marker only. No behaviour is implemented here yet; the
responsibility above is built in its owning phase, behind the contracts in
`alphakit-core`. Labeled stub — not a passed-off implementation.
"""

from __future__ import annotations

__version__: str = "0.0.1"
__all__: list[str] = ["__version__"]
